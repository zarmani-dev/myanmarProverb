<!DOCTYPE html>
<html lang="en">
    <%- include("./partials/header.ejs") %>
<body>
    <div
      class="container max-w-[700px] my-[10px] md:my-[50px] bg-white border-0 md:border border-black mx-auto p-[15px] rounded-sm font-walone tracking-wide relative"
    >
    <%- include("./partials/prevPage.ejs") %>
      <div class="flex">
        <img src="img/book.svg" class="w-6 mr-2" />
        <h1 class="">"<%= proverbTitle %>" (စကားပုံ)</h1>
      </div>
      <table class="w-full border-collapse border border-slate-400 mt-4">
        <tbody>
            <% proverbs.forEach((proverb, index) => { %>
                <tr>
                    <td class="border border-slate-300 p-1 cursor-pointer text-black" data-titleId="<%=proverb.TitleId %>" data-proverbId="<%= index + 1 %>">
                        <img src="img/pencil.svg" class="w-3 inline" alt="">
                        <%=  proverb.ProverbName %>
                    </td>
                </tr>
            <% }) %>
        </tbody>
      </table>
      <h1 class="font-bold text-center mt-4 text-[25px]">"<%= proverbTitle %>" အက္ခရာ ပြီး၏။</h1>
    </div>
    <script src="details.js"></script>
</body>
</html>